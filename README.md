# **Instructions:**

-- clone the repo
-- run index.html with liveserver

## How to import the workflow:

-- open up your instance of n8n
-- create a workflow
-- click 3 dots on top left
-- "import via file"
